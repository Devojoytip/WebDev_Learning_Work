// to see whole structure
// document is also a JS Object but it seems different than other objects so we can have comfort
console.log(document); 

// to see whole HTML structure
console.log(document.documentElement);

console.log(window);

console.log(window.screen);
